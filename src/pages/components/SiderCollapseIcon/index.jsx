import * as React from 'react';
import Icon from '@ant-design/icons';
import SiderArrowIcon from '../../../assets/icons/SiderArrowIcon';

export default function SiderCollapseIcon( {rotate} ) {
  return <Icon component={SiderArrowIcon} rotate={rotate} />;
}
